jobList.csv is a spreadsheet of the job lists
uids is a list of the uids, exported from the serial output of "dumpUIDs.ino" arduino sketch

cardList.h will be a header containing array of all the card data

run ./parsecsvtojoblist jobList.csv to generate joblist
run ./generatecardlist to generate a card header

